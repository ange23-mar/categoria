server:
  port: ${PORT:8081}
  address: 0.0.0.0

spring:
  application:
    name: ms-categorias

  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://eureka-server:8761/eureka/}
  instance:
    prefer-ip-address: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: alpha

    ##http://localhost:8081/api/categoria con el controller 1
##http://localhost:8087/categorias2 con el controller 2
##http://localhost:8081/swagger-ui/index.html con swagger para probar el controller 1
##http://localhost:8087/categorias/1 con el controller 