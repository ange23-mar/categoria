server:
  port: 8081
  address: 0.0.0.0

spring:
  application:
    name: ms-categorias

datasource:
  url: jdbc:oracle:thin:@localhost:1521:XE
  username: SCHEMA_CATEGORIAS
  password: FullStack-Bodega1
  driver-class-name: oracle.jdbc.OracleDriver

  #datasource:
   #url: jdbc:mysql://host.docker.internal:3306/db_ms_categorias
   #username: root
   #password: ""
   #driver-class-name: com.mysql.cj.jdbc.Driver
   
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
  instance:
    prefer-ip-address: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: alpha

    ##http://localhost:8081/api/categoria con el controller 1
##http://localhost:8087/categorias2 con el controller 2
##http://localhost:8087/swagger-ui/index.html con swagger para probar el controller 1
##http://localhost:8087/categorias/1 con el controller 